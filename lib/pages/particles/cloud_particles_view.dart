import 'package:flutter/material.dart';

class CloudParticlesView extends StatelessWidget {
  const CloudParticlesView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Cloud View'),
      ),
    );
  }
}
